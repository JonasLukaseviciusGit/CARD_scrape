import requests
import passwords

query = "pain management"
email = "geerwg743@gmail.com"

url = f"https://api.unpaywall.org/v2/search?query={query}&is_oa=true&email={passwords.email}"

response = requests.get(url).json()['results']

for el in response:
    print(el)

print(len(response))
